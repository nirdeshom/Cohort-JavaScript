let fruits = ["Apple", "Banana", "Mango", "Orange"];

// loop iteration 
// variable lekar aao 
// limit btao 
// incre / decre

for(let h = 0; h < fruits.length; h++){
    console.log(`fruits at index ${h} : ${fruits[h]}`);
}
